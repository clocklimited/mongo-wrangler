{
  "devDependencies": {
    "pkg": "^5.8.0"
  },
  "scripts": {
    "build": "yarn build-file restore.js && yarn build-file dump.js",
    "build-file": "pkg --target latest-linux,latest-macos --out-path dist --compress GZip"
  }
}
